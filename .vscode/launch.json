{
  "version": "0.2.0",
  "configurations": [
    {
      "request": "launch",
      "name": "Launch Bun Test Dynamically",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["test", "--inspect-brk", "${file}"],
      "console": "integratedTerminal"
    }
  ]
}
